# guy-installer

Thin shell over the bootstrap core.

Responsibilities:
- install or repair the runtime
- enroll the current device
- apply a selected profile
- switch release channels
- expose the same flows to a future desktop wrapper
